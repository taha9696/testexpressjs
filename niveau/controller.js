var express = require('express');
var router = express.Router();
const { addNiveau } = require('./niveauService');

// Route pour ajouter un niveau
router.post('/addNiveau', );

module.exports = router;
